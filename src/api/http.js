import { Platform } from "react-native";

const path = "http://speedHome.com";
const APIURL = `${path}/api`;
const registerEmailEndpoint = "registerEmail";

module.exports = {
  APIURL,
  registerEmailEndpoint
};
